<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="140dp"
    android:layout_height="wrap_content"
    android:layout_gravity="center"
    android:background="@android:color/transparent"
    android:orientation="horizontal">

    <ImageView
        android:id="@+id/teamLogoImageView"
        android:layout_width="@dimen/create_in_draw_team_size"
        android:layout_height="@dimen/create_in_draw_team_size"
        android:layout_margin="7dp"
        android:src="@drawable/tournament"
        app:strokeWidth="1dp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:orientation="vertical">

        <TextView
            android:id="@+id/teamNameTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Real Madrid"
            android:textColor="@color/white"
            android:textSize="@dimen/tournament_name_text_size"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/teamId"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Real Madrid"
            android:textColor="@color/white"
            android:textSize="@dimen/tournament_name_text_size"
            android:textStyle="bold"
            android:visibility="gone" />

    </LinearLayout>
</LinearLayout>

